#ifndef MemoryReader_DOT_H   /* This is an "include guard" */
#define MemoryReader_DOT_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/

/**
 * This is a function declaration.
 * It tells the compiler that the function exists somewhere.
 */
int hexadecimal2int(char *hdec);
int reader(char * pid, char * S_addr,char * E_addr);



#endif /* MemoryReader_DOT_H */